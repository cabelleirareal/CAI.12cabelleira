import React from 'react';

const Features: React.FC = () => {
  // This component is no longer rendered as its functionality has been
  // replaced by a direct button in the Hero component.
  return null;
};

export default Features;
